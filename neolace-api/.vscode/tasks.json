{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile neolace-api package for Node.js",
            "type": "shell",
            "command": "deno",
            "args": ["run", "--allow-read", "--allow-run", "--allow-write", "--allow-env", "--no-check", "_compile.ts"],
            "problemMatcher": []
        },
    ]
}
